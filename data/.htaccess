# Deny direct web access to the data folder to protect configurations and VCF files.
# Compatible with both Apache 2.2 and 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
